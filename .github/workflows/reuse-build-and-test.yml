name: Eggnine.TrashTaf_branch 

on: 
  workflow_call: 
    inputs: 
      operating-system: 
        required: true 
        type: string 
      browser: 
        required: true 
        type: string 
    secrets: 
      POSTGRES-PASSWORD: 
        required: true 
        type: string 
      MY-GITHUB-USERNAME: 
        required: true 
        type: string 
      MY-GITHUB-PASSWORD: 
        required: true 
        type: string 

env: 
    #for firefox on ubuntu 
    DISPLAY: :99 
    #for postgres 
    PGHOST: localhost 
    PGDATABASE: trash_db 
    PGUSERNAME: trash_owner 
    PGPASSWORD: ${{ secrets.POSTGRES-PASSWORD }} 

jobs: 
  build-and-test-trashtaf: 
    strategy: 
      fail-fast: false 
        
    runs-on: ${{ format('{0}', inputs.operating-system) }} 

    steps: 
#start building TrashTaf 
    - uses: actions/checkout@v4 
  
    - name: Setup .NET 
      uses: actions/setup-dotnet@v4 
      with: 
        dotnet-version: 8.0.x 

    - name: Restore dependencies 
      run: dotnet restore 

    - name: Build 
      run: dotnet build --no-restore 
#end building TrashTaf 

#start setting up postgres 
    - name: Add PostgreSQL binaries to PATH 
      shell: bash 
      run: | 
        if [ "$RUNNER_OS" == "Windows" ]; then 
          echo "$PGBIN" >> $GITHUB_PATH 
        elif [ "$RUNNER_OS" == "Linux" ]; then 
          echo "$(pg_config --bindir)" >> $GITHUB_PATH 
        fi 

    - name: Start preinstalled PostgreSQL 
      shell: bash 
      run: | 
        echo "Initializing database cluster..." 
        # Convert backslashes to forward slashes in RUNNER_TEMP for Windows Git Bash 
        export PGHOST="${RUNNER_TEMP//\\//}/postgres" 
        export PGDATA="$PGHOST/pgdata" 
        mkdir -p "$PGDATA" 

        # initdb requires file for password in non-interactive mode 
        export PWFILE="$RUNNER_TEMP/pwfile" 
        echo "postgres" > $PWFILE 
        initdb --pgdata="$PGDATA" --username="postgres" --pwfile="$PWFILE" 

        echo "Starting PostgreSQL..." 
        echo "unix_socket_directories = '$PGHOST'" >> "$PGDATA/postgresql.conf" 
        pg_ctl start 

        echo "Creating user..." 
        psql --host "$PGHOST" --username="postgres" --dbname="postgres" --command="CREATE USER $PGUSERNAME PASSWORD '$PGPASSWORD'" --command="\du" 

        echo "Creating database..." 
        createdb --owner="$PGUSERNAME" --username="postgres" "$PGDATABASE" 
        psql --host "$PGHOST" --username="$PGUSERNAME" --dbname="$PGDATABASE" < init.sql 
#end setting up postgress 

#start add support for Firefox on ubuntu 
    - name: Start Xvfb for Firefox
      if: ('$RUNNER_OS' == 'Linux' && ''${{ inputs.browser }}' == 'firefox') 
      run: Xvfb :99 & 
#end add support for Firefox on ubuntu 

#start write the appSettings.json 
    - uses: 1arp/create-a-file-action@0.4.5 
      with: 
        path: Eggnine.TrashTaf.XUnit/bin/Debug/net8.0/ 
        file: appSettings.json 
        content: > 
          { 
          "browserName": "${{ inputs.browser }}", 
          "browserMajorVersion": "latest", 
          "operatingSystemNameAndMajorVersion": "${{ inputs.operatingSystem }}", 
          "isHeadless": true, 
          "gitHubUsername": "${{ secrets.MY-GITHUB-USERNAME }}", 
          "gitHubPassword": "${{ secrets.MY-GITHUB-PASSWORD }}", 
          "databaseConnectionString": "host=localhost;database=trash_db;username=trash_owner;password=${{ secrets.POSTGRES-PASSWORD }};" 
          } 
#end write the appSettings.json 

#start test TrashTaf 
    - name: TrashTaf Test ${{ inputs.browser }} on ${{ inputs.operatingSystem }} 
      run: dotnet test --no-build --verbosity normal 
#end run TrashTaf 
