name: Eggnine.TrashTaf_montly_edge 

on:
  schedule:
  - cron: 0 18 1 */1 * 
  
jobs: 
  main: 
    uses: rf-eggnine/TrashTaf/.github/workflows/reuse-build-and-test.yml@main 
    with: 
      operating-system: ${{ matrix.operatingSystem }} 
      browser: edge 
    secrets: inherit 

    strategy: 
      fail-fast: false 
      matrix: 
        operatingSystem: [ windows-2019, windows-2022 ] 
